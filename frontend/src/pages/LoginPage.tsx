import{FormEvent,useState}from'react';import{useAuth}from'../context/AuthContext';
export default function LoginPage(){const{login,register}=useAuth();const[mode,setMode]=useState<'login'|'register'>('login');const[error,setError]=useState('');const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError('');const f=new FormData(e.currentTarget);try{if(mode==='login')await login(String(f.get('email')),String(f.get('password')));else await register({name:f.get('name'),email:f.get('email'),password:f.get('password'),password_confirmation:f.get('password'),timezone:Intl.DateTimeFormat().resolvedOptions().timeZone||'Europe/Vilnius'})}catch(e:any){setError(e.body?.message||e.message)}};return <div className="auth"><form className="card auth-card" onSubmit={submit}><div className="brand"><span>GYM</span> FIT</div><h1>{mode==='login'?'Welcome back':'Create account'}</h1>{mode==='register'&&<label>Name<input name="name" required/></label>}<label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label>{error&&<div className="error">{error}</div>}<button className="primary">{mode==='login'?'Sign in':'Create account'}</button><button type="button" className="ghost" onClick={()=>setMode(mode==='login'?'register':'login')}>{mode==='login'?'Need an account?':'Already have an account?'}</button></form></div>}
